# Java Spring Boot backend base URL.
# Leave empty when using Vite proxy or same-origin deployment.
VITE_API_BASE_URL=http://localhost:8080

# Keep true during frontend-only development.
# Set false when connecting to backend-java /api/auth/login, /api/auth/me and /api/auth/logout.
VITE_USE_MOCK_AUTH=true
